<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pingan.dynamicconfig">
  <application>
  <provider
      android:name="com.pingan.config.ContentProviderDb"
      android:authorities="${applicationId}"/>
  </application>
</manifest>
